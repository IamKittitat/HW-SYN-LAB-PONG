`timescale 1ns / 1ps

module score_counter(
);

// Store and calculate the score of both player, should not increase if score 99
// input: A_inc, B_inc, Reset
// output: A_scr (2 Digits of scroe, not sure what type BCD? binary? encoded?)
endmodule
