`timescale 1ns / 1ps

module core_logic(
);

// Main logic of the game, calculate physics, movement, EVERYTHING
// Calculate location of bar A,B
// Calculate location, velocity of ball
// Calculate if should increase score for player
// Take care case when start the game (ball)

// input: player control, reset
// output: Something useful for ui module, A_inc, B_inc
endmodule
